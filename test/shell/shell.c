
/*int main (){

	static SynchConsole *console;
	char * in, out, buffer[MAX_CHAR];
	console = new Console (in, out);

    while (1){
    	PutString("<Nachos_Shell>");	//affichage du prompt
    	do{// tant que l'on appuie pas sur entrée on lis les caractères entrés dans la console.
    		buffer[i] = console->SynchGetChar();	//on ajoute les caractères lu a buffer
    	}while (buffer[i++] != '\n');
    	buffer[--i] = '\0';
    	//on a finis de lire la commande entrée a la console
    	eval(buffer);
    }
}*/
